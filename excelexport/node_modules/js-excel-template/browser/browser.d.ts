import * as XLSX from "xlsx";
import JsExcelTemplateBase from "./common";
/**
 * @public
 */
export default class JsExcelTemplate extends JsExcelTemplateBase {
    static fromArrayBuffer(arrayBuffer: ArrayBuffer): JsExcelTemplate;
    toArrayBuffer(bookType: XLSX.BookType): ArrayBuffer;
    toBlob(bookType?: XLSX.BookType): Blob;
}
