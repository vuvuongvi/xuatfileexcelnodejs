import * as XLSX from "xlsx";
export default class JsExcelTemplateBase {
    protected workbook: XLSX.WorkBook;
    constructor(workbook: XLSX.WorkBook);
    set(name: string, value: any): void;
    private setCell(cell, name, value);
    private parseCellName(cellName);
    private findRowIndex(name, sheet);
}
